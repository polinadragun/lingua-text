# Public API base URL only. Do not put Yandex keys or any secrets here: REACT_APP_* is embedded in the browser bundle.
# Object Storage credentials belong in itmo-web-project/.env (server-side).
REACT_APP_API_URL=http://localhost:3001
